services:
  db:
    build:
      context: ./db
      dockerfile: Dockerfile
    ports:
      - "27017:27017"
    
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on: [db]
